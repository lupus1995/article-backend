export const maxLengthMessage = (item: string): string => (`The ${item} is short. Minimum 5 characters`);
export const minLengthMessage = (item: string): string => (`The ${item} is long. The maximum number of characters is 255`);
